# NavigationView

A NavigationView is a view that has the capability to navigate to other views.
Each NavigationView is comprised of 3 parts:

1. Name: A unique reference so that other views can navigate to your view.
2. View: The view to be rendered inside of the NavigationView.
3. Navigation Bar: A set of options to configure the navigation bar.
